import { Box } from '@mui/material';

const HexMap = () => (
  <Box
    component="img"
    src="./icon.png"
    alt="Hex Map"
    sx={{ width: '100%', maxWidth: 600, borderRadius: 4 }}
  />
);

export default HexMap;
